{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.invalid/make-my-resume-fit/metadata.schema.json",
  "title": "MakeMyResumeFit Resume Metadata",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "slug",
    "summary",
    "job_offer_urls",
    "target_files",
    "warnings",
    "changes"
  ],
  "properties": {
    "schema_version": {
      "type": "string"
    },
    "slug": {
      "type": "string",
      "description": "Deterministic job-offer slug in the form <company-token>-<role-token>, normalized with the prompt's Slug Rules.",
      "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$"
    },
    "summary": {
      "type": "string"
    },
    "job_offer_urls": {
      "type": "array",
      "description": "Supplied job-offer URLs, preserved exactly in CLI argument order.",
      "items": {
        "type": "string"
      }
    },
    "target_files": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "warnings": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "changes": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "id",
          "section",
          "location_hint",
          "change_type",
          "before",
          "after",
          "reason",
          "evidence",
          "truthfulness_risk"
        ],
        "properties": {
          "id": {
            "type": "string"
          },
          "section": {
            "type": "string"
          },
          "location_hint": {
            "type": "string"
          },
          "change_type": {
            "type": "string"
          },
          "before": {
            "type": "string"
          },
          "after": {
            "type": "string"
          },
          "reason": {
            "type": "string"
          },
          "evidence": {
            "type": "string"
          },
          "truthfulness_risk": {
            "type": "string"
          }
        }
      }
    }
  }
}
